[pytest]
; Garante que a raiz do projeto seja incluída no PYTHONPATH,
; permitindo que os testes importem "app.py" corretamente
; independentemente de onde o pytest for executado.
pythonpath = .
testpaths = tests
